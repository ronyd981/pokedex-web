// La API no debería estar a la vista, debería estar guardada en un archivo .env, pero en este caso es una API pública y no hay ningún problema en que sea vista
export const API_URI = "https://pokeapi.co/api/v2";
